- title "Professors"

.clear

#info
  %h3 Explore
  %p Curious as to what makes a great professor page?  Need inspiration?  Check out some examples from your peers to get a better idea.
  
- unless @users.count == 0
  = will_paginate @users
  %ul
    - @users.each do |user|
      %li.buttons= link_to user, user
- else
  #zero-count 
    %h3 Oops!  
    It looks like no professors have published their personas yet!

- if logged_in?  
  %h4 Enough inspiration?
  = link_to "Back to your persona", current_user, :class => "button positive"
