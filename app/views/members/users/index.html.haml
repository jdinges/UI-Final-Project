- title "Professors"

#info
  View information about a professor, including their biography, courses, and papers.

- unless @users.count == 0
  %ul
    - for user in @users
      %li= link_to "#{user.title} #{user.name}", members_user_path(user)
- else
  #zero-count No professors have used this application yet!
